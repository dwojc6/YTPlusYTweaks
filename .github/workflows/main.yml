name: Build YTPlusYTweaks

on:
  workflow_dispatch:
    inputs:

      enable_youpip:
        description: "Integrate YouPiP"
        type: boolean
        required: true
        default: true

      enable_ytuhd:
        description: "Integrate YTUHD"
        type: boolean
        required: true
        default: true

      enable_yq:
        description: "Integrate YouQuality"
        type: boolean
        required: true
        default: true

      enable_ryd:
        description: "Integrate Return YouTube Dislikes"
        type: boolean
        required: true
        default: true

      enable_demc:
        description: "Integrate DontEatMyContent"
        type: boolean
        required: true
        default: true

      enable_ytabconfig:
        description: "Integrate YTABConfig"
        type: boolean
        required: true
        default: true

      enable_ytweaks:
        description: "Integrate YTweaks"
        type: boolean
        required: true
        default: true

      enable_yticons:
        description: "Integrate YTIcons"
        type: boolean
        required: true
        default: false

      enable_gonerino:
        description: "Integrate Gonerino"
        type: boolean
        required: true
        default: false

      enable_yougroupsettings:
        description: "Group Tweak Settings (YouGroupSettings)"
        type: boolean
        required: true
        default: true

      ipa_url:
        description: "URL to the decrypted IPA file"
        default: ""
        required: true
        type: string

      tweak_version:
        description: "The version of the tweak to use (optional - leave empty to use latest from dayanch96/YTLite/releases)"
        default: ""
        required: false
        type: string

      sdk_version:
        description: "iOS SDK Version"
        type: choice
        options:
          - 16.5
          - 17.5
          - 18.6
        required: true
        default: 16.5

      display_name:
        description: "App Name (Optional)"
        default: "YouTube"
        required: true
        type: string

      bundle_id:
        description: "BundleID (Optional)"
        default: "com.google.ios.youtube"
        required: true
        type: string

      info_note:
        description: "TIP: Learn more about integrations in the README via the link below"
        default: "https://github.com/dayanch96/YTLite#tweak-integration-details"
        type: string
        required: false

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: Build YouTube Plus
    runs-on: macos-latest
    permissions:
      contents: write

    steps:
      - name: Checkout Main
        uses: actions/checkout@v4.1.1
        with:
          path: main
          submodules: recursive

      - name: Hide sensitive inputs
        uses: levibostian/action-hide-sensitive-inputs@v1
        with:
          exclude_inputs: bundle_id,display_name,enable_demc,enable_ryd,enable_youpip,enable_yq,enable_ytabconfig,enable_ytweaks,enable_yticons,enable_yougroupsettings,enable_ytuhd,enable_gonerino,info_note,tweak_version,sdk_version

      - name: Download and validate IPA
        run: |
          wget "${{ inputs.ipa_url }}" --no-verbose -O ${{ github.workspace }}/youtube.ipa

          file_type=$(file --mime-type -b ${{ github.workspace }}/youtube.ipa)

          if [[ "$file_type" != "application/x-ios-app" && "$file_type" != "application/zip" ]]; then
            echo "::error::Validation failed: The downloaded file is not a valid IPA. Detected type: $file_type."
            exit 1
          fi

      - name: Install Dependencies
        run: brew install make ldid

      - name: Install cyan
        run: pipx install --force https://github.com/asdfzxcvbn/pyzule-rw/archive/main.zip

      - name: Get latest YTLite version
        id: ytlite_version
        run: |
          if [ -z "${{ inputs.tweak_version }}" ]; then
            echo "Fetching latest release from dayanch96/YTLite..."
            
            # Use GitHub API with authentication token (automatically available in GitHub Actions)
            LATEST_TAG=$(python3 -c "
            import json, urllib.request, sys, os
            try:
                token = os.environ.get('GITHUB_TOKEN', '')
                req = urllib.request.Request('https://api.github.com/repos/dayanch96/YTLite/releases/latest')
                req.add_header('Authorization', f'Bearer {token}')
                req.add_header('Accept', 'application/vnd.github.v3+json')
                with urllib.request.urlopen(req) as response:
                    data = json.loads(response.read().decode())
                    tag = data.get('tag_name', '')
                    if tag:
                        print(tag)
                    else:
                        print('No tag_name in response', file=sys.stderr)
                        sys.exit(1)
            except urllib.error.HTTPError as e:
                print(f'HTTP Error {e.code}: {e.reason}', file=sys.stderr)
                if e.code == 404:
                    print('Release not found', file=sys.stderr)
                sys.exit(1)
            except Exception as e:
                print(f'Error: {e}', file=sys.stderr)
                sys.exit(1)
            ")
            
            if [ -z "$LATEST_TAG" ]; then
              echo "::error::Failed to fetch latest release tag from GitHub API"
              exit 1
            fi
            
            # Keep the full tag for version (e.g., v5.2b4 -> v5.2b4)
            # Remove 'v' prefix for the version variable (e.g., v5.2b4 -> 5.2b4)
            VERSION="${LATEST_TAG#v}"
            echo "Latest version found: $VERSION (tag: $LATEST_TAG)"
            echo "version=$VERSION" >> $GITHUB_OUTPUT
            echo "tag=$LATEST_TAG" >> $GITHUB_OUTPUT
          else
            echo "Using provided version: ${{ inputs.tweak_version }}"
            echo "version=${{ inputs.tweak_version }}" >> $GITHUB_OUTPUT
            echo "tag=v${{ inputs.tweak_version }}" >> $GITHUB_OUTPUT
          fi
        env:
          GITHUB_TOKEN: ${{ github.token }}

      - name: Download YouTube Plus
        id: download_ytp
        run: |
          deb_url="https://github.com/dayanch96/YTLite/releases/download/v${{ steps.ytlite_version.outputs.version }}/com.dvntm.ytlite_${{ steps.ytlite_version.outputs.version }}_iphoneos-arm.deb"
          echo "Downloading YouTube Plus version: ${{ steps.ytlite_version.outputs.version }}"
          wget "$deb_url" --no-verbose -O ${{ github.workspace }}/ytplus.deb

      - name: Clone Open in YouTube (Safari extension)
        run: |
          git clone --quiet -n --depth=1 --filter=tree:0 https://github.com/CokePokes/YoutubeExtensions/
          cd YoutubeExtensions
          git sparse-checkout set --no-cone OpenYoutubeSafariExtension.appex
          git checkout
          mv *.appex ${{ github.workspace }}

      - name: Copy pre-built DEB files
        run: |
          echo "📦 Copying pre-built DEB files from main/deb..."
          cd ${{ github.workspace }}
          
          # Create a mapping of enable flags to deb filenames
          declare -A deb_mapping=(
            ["enable_youpip"]="youpip.deb"
            ["enable_ytuhd"]="ytuhd.deb"
            ["enable_yq"]="yq.deb"
            ["enable_ryd"]="ryd.deb"
            ["enable_yougroupsettings"]="ygs.deb"
            ["enable_ytabconfig"]="yabc.deb"
            ["enable_ytweaks"]="ytwks.deb"
            ["enable_yticons"]="yticons.deb"
            ["enable_demc"]="demc.deb"
            ["enable_gonerino"]="gonerino.deb"
          )
          
          # Copy enabled tweaks
          if [ "${{ inputs.enable_youpip }}" = "true" ] && [ -f "main/deb/youpip.deb" ]; then
            echo "✅ Copying YouPiP"
            cp main/deb/youpip.deb ./youpip.deb
          fi
          
          if [ "${{ inputs.enable_ytuhd }}" = "true" ] && [ -f "main/deb/ytuhd.deb" ]; then
            echo "✅ Copying YTUHD"
            cp main/deb/ytuhd.deb ./ytuhd.deb
          fi
          
          if [ "${{ inputs.enable_yq }}" = "true" ] && [ -f "main/deb/yq.deb" ]; then
            echo "✅ Copying YouQuality"
            cp main/deb/yq.deb ./yq.deb
          fi
          
          if [ "${{ inputs.enable_ryd }}" = "true" ] && [ -f "main/deb/ryd.deb" ]; then
            echo "✅ Copying Return YouTube Dislikes"
            cp main/deb/ryd.deb ./ryd.deb
          fi
          
          if [ "${{ inputs.enable_yougroupsettings }}" = "true" ] && [ -f "main/deb/ygs.deb" ]; then
            echo "✅ Copying YouGroupSettings"
            cp main/deb/ygs.deb ./ygs.deb
          fi
          
          if [ "${{ inputs.enable_ytabconfig }}" = "true" ] && [ -f "main/deb/yabc.deb" ]; then
            echo "✅ Copying YTABConfig"
            cp main/deb/yabc.deb ./yabc.deb
          fi
          
          if [ "${{ inputs.enable_ytweaks }}" = "true" ] && [ -f "main/deb/ytwks.deb" ]; then
            echo "✅ Copying YTweaks"
            cp main/deb/ytwks.deb ./ytwks.deb
          fi
          
          if [ "${{ inputs.enable_yticons }}" = "true" ] && [ -f "main/deb/yticons.deb" ]; then
            echo "✅ Copying YTIcons"
            cp main/deb/yticons.deb ./yticons.deb
          fi
          
          if [ "${{ inputs.enable_demc }}" = "true" ] && [ -f "main/deb/demc.deb" ]; then
            echo "✅ Copying DontEatMyContent"
            cp main/deb/demc.deb ./demc.deb
          fi
          
          if [ "${{ inputs.enable_gonerino }}" = "true" ] && [ -f "main/deb/gonerino.deb" ]; then
            echo "✅ Copying Gonerino"
            cp main/deb/gonerino.deb ./gonerino.deb
          fi
          
          # Copy YTVideoOverlay if needed by YouQuality or YouPiP
          if [ "${{ inputs.enable_yq }}" = "true" ] || [ "${{ inputs.enable_youpip }}" = "true" ]; then
            if [ -f "main/deb/ytvo.deb" ]; then
              echo "✅ Copying YTVideoOverlay"
              cp main/deb/ytvo.deb ./ytvo.deb
            fi
          fi
          
          echo ""
          echo "📋 DEB files ready:"
          ls -lh *.deb 2>/dev/null || echo "No DEB files found"

      - name: Inject tweaks into IPA
        run: |
          tweaks="ytplus.deb OpenYoutubeSafariExtension.appex"

          for f in *.deb; do
            if [ -f "$f" ]; then
              tweaks="$tweaks $f"
            fi
          done
          
          echo "🔧 Injecting tweaks: $tweaks"
          cyan -i youtube.ipa -o YTPlusYTweaks_${{ steps.ytlite_version.outputs.version }}.ipa -uwef $tweaks -n "${{ inputs.display_name }}" -b ${{ inputs.bundle_id }}

      - name: Upload to GitHub Releases
        uses: softprops/action-gh-release@v2.0.1
        with:
          tag_name: ytp-${{ github.run_number }}
          name: YouTubePlus v${{ steps.ytlite_version.outputs.version }} (${{ github.run_number }})
          files: YTPlusYTweaks_${{ steps.ytlite_version.outputs.version }}.ipa
          draft: true

      - name: Output Release URL
        run: |
          echo "::notice::Release available at: https://github.com/${{ github.repository }}/releases"
